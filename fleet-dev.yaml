# Optional Fleet options file for a separate dev GitRepo using path: .
defaultNamespace: connectme-dev
kustomize:
  dir: overlays/dev
helm:
  releaseName: connectme-dev
  timeoutSeconds: 300
keepResources: true
