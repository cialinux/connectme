apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - postgres.yaml
  - service.yaml
  - networkpolicy.yaml
configMapGenerator:
  - name: connectme-ssh-trust
    files:
      - ssh_known_hosts
