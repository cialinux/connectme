apiVersion: apps/v1
kind: Deployment
metadata: {name: connectme}
spec:
  template:
    spec:
      nodeSelector: {workload: pro}
      tolerations:
        - {key: dr, operator: Exists, effect: NoSchedule}
