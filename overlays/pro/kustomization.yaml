apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: connectme-pro
# Independent Fleet bundle. Workloads are owned by the separate base bundle.
resources:
  - namespace.yaml
  - ingress.yaml
