apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: connectme-pro
resources:
  - namespace.yaml
#  - ../../base
  - ingress.yaml
patches:
  - path: scheduling.yaml
  - path: postgres-scheduling.yaml
images:
  - name: virgiliofilhos/connectme-server
    # Pin a tested version/digest before production rollout. No automatic deploy.
    newTag: latest
